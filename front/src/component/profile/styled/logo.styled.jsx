import styled from "styled-components"

export const ProfileLogo = styled.div`
    width: fit-content;
    margin: 8rem auto 0 auto;
    & > p {
        /* display: block; */
        margin: 0 auto;
        color: #444444;
        font-size: 1.6rem;
    }
`
